# 腾讯-应用宝-大模型一面
自我介绍
本科不是计算机
手撕 路径总和2

手撕环节题目没读清楚直接就写了，有两个bug，边界条件没有写清楚，一个是到子节点，一个是node是none
解释一下 BFS 跟 DFS 的区别？用什么数据结构来实现？

详细讲讲京东意图分发模型的 SFT 训练。
（这个其实应该是先说一下agent的内容和背景，然后从数据处理开始一点一点说，即注重这个实操）

原来 Base 是用什么样的模型？
你说是 235B 的模型。用 30B 的模型通过 SFT 去替代 235B 的大模型，这是一个极其经典的“小模型蒸馏/替换大模型以降低线上推理成本”的故事。下次再讲这个项目，除了强调准确率提升至 94%，一定要补充一句：“用 30B 的专属模型替换了 235B 的通用大模型，在准确率不降反升的前提下，极大地节省了线上 GPU 显存占用和推理耗时。




路由模型输出的是什么格式？
直接说输出的是一段很短的json就行


模型训练的样本是怎么生成的？
这里答的也不是很好，数据增强要从几个方面来
Prompt 是怎么写的？（是否用到了 In-context Learning 给出正反例？）
数据清洗过滤规则是什么？（如何剔除 GPT 生成的格式错误或胡言乱语？）
数据分布是怎样的？（那 12 个细分标签的比例如何，有没有针对长尾标签做过采样？）


做这个项目的核心目标是为了划清业务边界，还是整体提高准确率？ （这个答的也不是很好，其实应该说提升线上的准确率）
这个其实是我自己说的不清楚，以后在说项目的时候要一点一点去说，


测试的时候只测了这 1 万条吗？有没有回归线上数据？
预估一下微调这个 30B 模型需要多大显存？（这个没答上来）



部署的时候 30B 有做量化吗？你知道哪些量化算法？（这个没答上来）
这里可以这么说，就是
我们上线的模型会上传到一个京东自己的云平台上，
“虽然最终上线的工程封装由后端负责，但我们在交付模型时，测试了基于 vLLM 框架的 FP16 推理，并发量在 X 的情况下，首字延迟（TTFT）和生成延迟能够稳定在 200ms 内。


SFT 训练的损失函数是什么？公式是什么样的？（这个没答上来，只说了一点点）
